// No-op stub for the `server-only` package under vitest (node has no
// react-server condition, so the real package throws). The boundary it
// enforces is verified by `next build`, not by unit tests.
export {};
